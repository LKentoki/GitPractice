<section>标签：用于有明确语义的内容区块，通常有标题

<div>标签：用于纯样式分组，没有语义意义

使用场景：
<section>:将相关内容组织成独立章节时使用，或多层章节时嵌套使用（如书籍的章→节），或当内容需明确主题且其他语义标签（如<article>、<nav>）不适用时使用；
<div>:用于纯布局与样式化，或作为CSS或JavaScript操作的通用容器，或用于动态更新内容或绑定事件，或用于​​无主题关联的分组​​（如包裹按钮组、装饰性元素）